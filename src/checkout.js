/**
 * Checkout
 * ------------------------------------------------------------------------------
 * Creates and adds to cart.
 *
 * @namespace checkout
 *
 */
export default () => {

  /**
   * Init the checkout.
   */
  function init() {

  }

  return Object.freeze({
    init,
  })
}